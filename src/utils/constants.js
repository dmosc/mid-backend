const defaultExpiresIn = '2d';
const roles = ['ADMIN', 'VIEWER'];
const states = ['INITIATED', 'IN_PROGRESS', 'FINISHED', 'CANCELLED'];
const events = ['MEETING', 'FILE_UPLOAD', 'FILE_DELETE', 'STATE_CHANGE'];

export { defaultExpiresIn, roles, states, events };
